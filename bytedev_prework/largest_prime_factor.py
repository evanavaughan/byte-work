#The largest prime factor of a integer is the largest prime number than can divide the integer into another integer.
#Write a function that will take an integer as an argument.
#The function will return the largest prime factor of the passed in integer.
#If the integer is prime itself, then it is the largest prime factor of itself.
'''
x = int(input("Give me an integer:\n"))

get largest prime factor of x

nested for loops

while x > 0:
    y = (x-1)
    if x/y !=
    x = x-1
'''
primes = []

def test_prime(n):
    if (n==1):
        primes.append()
    elif (n==2):
        return primes.append()
    else:
        for x in range(2,n):
            if(n % x==0):
                return False
        return primes.append()          
print(test_prime(33))
print(primes)